import { ExtractOtherNamesPipe } from './extract-other-names.pipe';

describe('ExtractOtherNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractOtherNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
